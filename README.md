# ROBLOX PORTO - GAME DESIGN
Generated by [Rojo](https://github.com/rojo-rbx/rojo) 7.6.1.

## Proposed Architecture (Social System Sandbox)

```text
src/
	client/
		init.client.luau
		Bootstrap.luau
		Controllers/
			HubController.luau
		Modules/
			ModuleLoader.luau
			ModuleRegistry.luau
		State/
			HubContext.luau
		UI/
			mountHub.luau
			App.luau
			HubMenu.luau
			Components/
				ModuleButton.luau
	server/
		init.server.luau
		Bootstrap.luau
		Services/
			SandboxService.luau
	shared/
		Config/
			ModuleIds.luau
```

### Responsibilities
- Client/UI: React-Roblox Hub menu, module selection UX, and local session state.
- Client/Controllers: Event wiring between UI actions and module initialization.
- Client/Modules: Loader signal + registry stubs for each prototype environment.
- Server/Services: Reserved orchestration layer for future module server logic.
- Shared/Config: Shared identifiers/constants used by both client and server.

## Dependencies

The project uses Wally for package management and expects a `Packages/` folder synced to `ReplicatedStorage` via Rojo.

```bash
wally install
rojo serve
```

## Getting Started
To build the place from scratch, use:

```bash
rojo build -o "ROBLOX PORTO - GAME DESIGN.rbxlx"
```

Next, open `ROBLOX PORTO - GAME DESIGN.rbxlx` in Roblox Studio and start the Rojo server:

```bash
rojo serve
```

For more help, check out [the Rojo documentation](https://rojo.space/docs).